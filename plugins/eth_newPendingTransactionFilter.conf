parameters=
fee=0
clientrequestlimit=50
disabled=0
help=Creates a filter in the node, to notify when new pending transactions arrive. To check if the state has changed, call eth_getFilterChanges. Usage: xrService eth_newPendingTransactionFilter
