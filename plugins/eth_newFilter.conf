parameters=string,string,string,string
fee=0
clientrequestlimit=50
disabled=0
help=Creates a filter object, based on filter options, to notify when the state changes (logs). To check if the state has changed, call eth_getFilterChanges. Note: '[topics]' Requires JSON array of data enclosed in square brackets with single quotes. Usage: xrService eth_newFilter [fromBlock] [toBlock] [address] ['[topics]']
